# ============================================================================
# StreamMart Python Dependencies
# ============================================================================
# IMPORTANT: Requires Python 3.10 or 3.11 (NOT 3.13 or newer)
# Many data engineering packages don't support Python 3.13+ yet
# ============================================================================

# Core
python-dotenv==1.0.0

# Event Generation
Faker==22.0.0
confluent-kafka[avro]==2.3.0
avro-python3==1.10.2

# Data Processing (only needed for local dev, Spark/Airflow run in Docker)
# Uncomment if you need to develop Spark jobs locally:
# pyspark==3.5.0
# pandas>=2.2.0  # Use newer version for pre-built Windows wheels
# numpy>=1.26.0

# Database Clients
psycopg2-binary==2.9.9
SQLAlchemy==2.0.25

# Object Storage
minio==7.2.3
boto3==1.34.18

# Apache Airflow (runs in Docker, uncomment only if needed locally)
# apache-airflow==2.8.0
# apache-airflow-providers-postgres==5.9.0
# apache-airflow-providers-apache-spark==4.6.0

# Monitoring & Observability
prometheus-client==0.19.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0

# Utilities
requests==2.31.0
PyYAML==6.0.1
click==8.1.7

# Code Quality
black==23.12.1
flake8==7.0.0
mypy==1.8.0
